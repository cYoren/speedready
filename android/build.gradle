// Root build file. Versions live here so F-Droid can see exactly what is used.
buildscript {
    repositories { google(); mavenCentral() }
    dependencies { classpath "com.android.tools.build:gradle:8.7.3" }
}
